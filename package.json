{
  "name": "guanxin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "preview": "next build && wrangler pages dev .vercel/output/static",
    "deploy": "next build && wrangler pages deploy .vercel/output/static",
    "pages:build": "npx @cloudflare/next-on-pages"
  },
  "dependencies": {
    "next": "16.0.7",
    "react": "^18",
    "react-dom": "^18",
    "@vercel/analytics": "^1.3.1",
    "posthog-js": "^1.240.4"
  },
  "devDependencies": {
    "@cloudflare/next-on-pages": "^1.13.12"
  }
}
